*. Spring 코어
    1. 빈 컨테이너
    2. 유틸리티

*. Spring 컨텍스트
    1. ApplicationContext
    2. UI
    3. 유효성 검증(validation)
    4. JNDI
    5. 엔터프라이즈 자바빈즈(Enterprise JavaBeans, EJB)
    6. 리모팅(remoting)
    7. 메일

*. Spring DAO
    1. 트랜잭션 인프라
    2. JDBC(Java Database Connectivity)
    3. DAO(Data Access Object)

*. Spring ORM
    1. 하이버네이트(Hibernate)
    2. 아이바티스(iBATIS)
    3. JDO(Java Data Objects)

*. Spring AOP
    1. AOP 얼라이언스 호환(Alliance–compliant)
    2. 관점 지향 프로그래밍(aspect-oriented programming, AOP) 구현

*. Spring Web
    1. 멀티파트 처리
    2. 서블릿 리스너를 통한 컨텍스트 초기화
    3. 웹 애플리케이션 컨텍스트와 같은 기본적인 통합 기능

*. Spring Web MVC
    1. 웹 기반 MVC(Model-View-Controller) 모델